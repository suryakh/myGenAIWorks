# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a GenAI application project in early development. The repository is hosted at `git@github.com:suryakh/myGenAIWorks.git`.

## Current State

The project has no build system, dependencies, or source code yet. As the project grows, update this file with:

- Build/run commands (e.g., `npm install && npm start` or `pip install -r requirements.txt && python main.py`)
- Lint commands (e.g., `npm run lint` or `ruff check .`)
- Test commands (e.g., `npm test` or `pytest`)
- Architecture overview once the structure is established
